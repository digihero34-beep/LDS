# Database URL (SQLite for local zero-config execution; or PostgreSQL connection string in production)
DATABASE_URL="file:./dev.db"

# Optional LLM API Key (e.g. Gemini, OpenAI, Claude, or OpenRouter)
LLM_API_KEY=""
LLM_PROVIDER="gemini"
LLM_MODEL="gemini-2.5-flash"
